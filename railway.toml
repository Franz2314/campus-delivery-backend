[variables]
# Usar DATABASE_URL es más seguro que credenciales sueltas
# DATABASE_URL = "postgresql://user:password@host:5432/dbname?sslmode=require"
NODE_ENV = "production"
JWT_SECRET = "campus-delivery-jwt-produccion-2026"
JWT_EXPIRES_IN = "7d"

[deploy]
startCommand = "node src/app.js"
healthcheckPath = "/api/health"
